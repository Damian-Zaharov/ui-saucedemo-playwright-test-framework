[pytest]
# Оставляем только настройки Allure отчетов и chromium
addopts = --alluredir=allure-results --clean-alluredir --browser=chromium -n auto
testpaths = tests
markers =
    smoke: критические тесты для проверки базовой работоспособности
    regression: регрессионные тесты
